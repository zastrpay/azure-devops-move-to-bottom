# Move to Bottom (Azure DevOps Backlog)

A small private Azure DevOps extension that adds a **Move to bottom** command to the
**backlog item context menu** in Azure Boards — the mirror image of the built-in
**Move to top**.

Right-click one or more items on a backlog → **Move to bottom** → they jump to the end
of the backlog. No dragging, no typing a number into *Move to position…*.

---

## How it works

Azure DevOps lets extensions contribute context-menu actions. This extension contributes
a single [`ms.vss-web.action`](https://learn.microsoft.com/en-us/azure/devops/extend/develop/add-action)
targeting `ms.vss-work-web.backlog-item-menu`. The handler (`src/move-to-bottom.ts`, bundled
into `scripts/move-to-bottom.js`) registers with the
[`azure-devops-extension-sdk`](https://www.npmjs.com/package/azure-devops-extension-sdk) and,
when clicked, calls the Work REST client's `reorderBacklogWorkItems`
with a [`ReorderOperation`](https://learn.microsoft.com/en-us/javascript/api/azure-devops-extension-api/reorderoperation)
of `{ ids, previousId: null, nextId: 0 }`.

`nextId: 0` means *"place after the last item"* — i.e. the bottom — exactly mirroring how
**Move to top** uses `previousId: 0`. Because the boundary is expressed with `0`, there is
no need to look up the current last item; the service computes the new order. The handler
then reloads the page so the new order shows immediately.

> **Note on placement:** Azure DevOps renders extension-contributed actions in their own
> group within the context menu (typically near the bottom), so **Move to bottom** appears
> as a custom action rather than directly beside the native **Move to top**. The behavior is
> identical; only the menu position differs.

## Repository layout

```
vss-extension.json          Extension manifest (id, publisher, scopes, the menu contribution)
overview.md                 Marketplace description (manifest `content.details`)
src/
  move-to-bottom.ts         Menu-action handler: registers with the VSS SDK, calls the reorder API
scripts/
  move-to-bottom.html       Hidden host page that loads the bundled handler
  move-to-bottom.js         Webpack bundle of src/ (generated by `npm run build`, gitignored)
images/
  logo.png                  128x128 Marketplace icon
  move-to-bottom.png        32x32 menu icon
webpack.config.js           Bundles src/move-to-bottom.ts -> scripts/move-to-bottom.js
tsconfig.json               TypeScript compiler options
package.json                Build/publish npm scripts + dev dependencies
```

## Prerequisites

- **Node.js** (any current LTS) and npm.
- A **Visual Studio Marketplace publisher** you own. Create one (one-time) at
  <https://marketplace.visualstudio.com/manage> and note its **publisher ID**.
- Permission to **administer extensions** in the target Azure DevOps organization
  (Project Collection Administrator / organization Owner) to install the shared extension.

## 1. Configure the manifest

Open `vss-extension.json` and set **`publisher`** to your Marketplace publisher ID
(currently `"zastrpay"` — change it if your publisher ID differs):

```json
"publisher": "zastrpay",
```

`"public": false` keeps the extension **private**; it is only visible to organizations you
explicitly share it with.

## 2. Build the `.vsix`

```bash
npm install
npm run package      # compiles the TypeScript handler (webpack), then `tfx extension create`
```

This produces `dist/<publisher>.move-to-bottom-1.0.0.vsix`.

## 3. Publish privately and share with your organization

**Option A — Marketplace web UI (recommended for the first publish)**

1. Go to <https://marketplace.visualstudio.com/manage/publishers/<your-publisher>>.
2. **New extension → Azure DevOps** and upload the `.vsix` from `dist/`.
3. The extension is **private** by default. Open it → **Share/Unshare** → add the
   organization(s) you want to share it with.

**Option B — command line (tfx)**

Create a Personal Access Token with the **Marketplace → Manage** scope, then:

```bash
npx tfx extension publish \
  --manifest-globs vss-extension.json \
  --share-with <your-org> \
  --token <YOUR_MARKETPLACE_PAT>
```

(`--share-with` accepts multiple orgs, space-separated.)

## 4. Install into the organization

1. Azure DevOps → **Organization settings → Extensions → Shared** tab.
2. The shared **Move to Bottom (Backlog)** extension appears there → **Install** →
   choose the organization.

(Alternatively, from the shared extension's Marketplace page, **Get it free → Install →**
select your organization.)

## 5. Use it

Open any backlog (Boards → Backlogs), right-click one or more work items, and choose
**Move to bottom**. The backlog reloads with the item(s) at the end.

---

## Updating the extension

1. Bump `version` in `vss-extension.json` (e.g. `1.0.1`).
2. `npm run package` and re-upload the new `.vsix` (or `npm run publish`).

Installed organizations pick up the new version automatically.

## Troubleshooting

| Symptom | Fix |
| --- | --- |
| Menu item doesn't appear | Confirm the extension is **installed** (not just shared) in the org, and hard-refresh the backlog. Contribution target IDs are case-sensitive. |
| "Move to bottom" does nothing | The current view must have a **team** in scope (a normal backlog does). Check the browser console in the hidden iframe for REST errors. |
| Publish rejected | The `publisher` in the manifest must match a Marketplace publisher you own, and your PAT needs the **Marketplace → Manage** scope. |
| Item lands above other items | Reordering acts on the team's backlog order; items filtered out of the current backlog view are not reordered. |

## Scopes

`vso.work_write` — read & write work items, required to reorder the backlog.

## License

MIT — see [LICENSE](LICENSE).
